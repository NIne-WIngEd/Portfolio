# How to open

Do not preview from inside the ZIP. Extract the folder first.

Recommended local preview:

```bash
cd MK_Rayan_SignalSystem_Story_Portfolio_2026
python -m http.server 8000
```

Then open `http://localhost:8000`.

For quick checking, open `OPEN_PREVIEW_HOMEPAGE.html`.
